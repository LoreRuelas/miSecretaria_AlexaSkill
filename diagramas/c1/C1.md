```plantuml
@startuml
!include https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Context.puml

title C1 – Alexa Skill Context

Person(user, "User", "Persona que habla con Alexa")

System(system, "Alexa Skill", "Código Lambda que maneja intents")

System_Ext(alexa, "Alexa Platform", "Reconoce voz y envía intents")
System_Ext(aws, "AWS Lambda Runtime", "Ejecuta tu Skill")

user --> alexa : Voz
alexa --> system : Intents / Requests
system --> alexa : Respuestas

@enduml

```
